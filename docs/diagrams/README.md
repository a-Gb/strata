# Diagram sources

These files are standalone Mermaid diagrams duplicated from or complementing the architecture documents. They are intentionally kept as text so they can be versioned, reviewed, and rendered by any Mermaid-compatible tool.

# strata-source

Immutable bounded byte-source interfaces with retained-memory and local-file
implementations. Local files support generation checks, random range reads,
stable identity, and progressive hashing without requiring whole-file
allocation.

# strata-observability

Source-safe tracing, metrics, and diagnostic snapshot contracts. Dynamic labels
must not contain local paths or source-derived content. Concrete OSLog, trace
storage, and support-bundle implementations are future host work.

# strata-plugin-api

Host-side Rust types mirroring the versioned WIT plugin contract: manifests,
capability requests, bounded source handles, and validated outputs. This crate
defines a boundary; it does not execute untrusted components.

# strata-core

Backend-neutral domain contracts shared by every Strata layer. This crate owns
checked address/range types, immutable identities, selections, analysis
descriptors, transforms, and view specifications; it intentionally has no UI,
OS, GPU, parser, or storage dependency.

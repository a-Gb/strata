# strata-gpu

Backend-neutral GPU job and budget contracts plus the WGPU implementation used
for Alignment and Hamming projection coordinates. The live backend validates
resource limits and is acceptance-tested against deterministic CPU references.
Other planned kernels remain clearly marked specifications under `shaders/`.

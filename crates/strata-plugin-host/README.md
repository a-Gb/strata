# strata-plugin-host

Capability, quota, lifecycle, and installation contracts for an isolated plugin
host. Third-party component execution is not enabled in the current product
host; callers must not treat these interfaces as a deployed sandbox.
